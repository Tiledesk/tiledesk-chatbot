var assert = require('assert');
const { LanguageChooser } = require('../LanguageChooser');

const BOTS = [];

describe('chooser', function() {
  
    

    it('chooser Da', async () => {
        chooser = new LanguageChooser();
        const bot = chooser.findIn(bots)        
        assert(intent === null);
    });
    
});



